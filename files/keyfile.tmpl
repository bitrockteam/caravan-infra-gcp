{{ $host := printf "common_name=%s.node.gcp-dc.consul" (env "HOSTNAME") }}{{ with secret "tls_pki_int/issue/consul" $host "alt_names=client.gcp-dc.consul,localhost" "ip_sans=127.0.0.1" "ttl=720h"}}{{ .Data.private_key }}{{ end }}